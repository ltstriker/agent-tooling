@AGENTS.md

<!-- Claude Code inlines AGENTS.md through the import above; add Claude-specific
     instructions below this line. Other hosts read AGENTS.md directly. -->

Use plan mode before touching the Git gates (`plugins/boxlite-agent-tooling/.githooks/`) — a broken gate blocks every consumer's commits, not just this repo's.
